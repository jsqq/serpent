/**
 * Created by Дмитрий on 01.02.2018.
 */
'use strict';


function Drawer() {
    var draw = function (arrayOfCells) {
        alert(222);
        // // draw func there
        // arrayOfCells.forEach(function(item, i, arrayOfCells) {
        //     alert( i + ": " + item + " (массив:" + arr + ")" );
        // });
    }
}

var drawer = new Drawer();